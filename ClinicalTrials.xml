<?xml version="1.0" encoding="UTF-8" ?> 
<Module>
  <ModulePrefs title="Demo2" description="Demo2"> 
    <Require feature="opensocial-0.9"/>
    <Require feature="views"/>
    <Require feature="osapi"/>
    <Require feature="dynamic-height"/>
    <Require feature="jsonld"/>
    <Require feature="orng"/>
  </ModulePrefs>

  <Content type="html" view="default, home, profile">
    <![CDATA[
      <!DOCTYPE html>
      <link rel="stylesheet" type="text/css" href="mystyle.css">
      <script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js"></script>
      <script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jqueryui/1.11.1/jquery-ui.min.js"></script>
      <script type="text/javascript" src="js/environment.js" ></script>
      <script type="text/javascript" src="js/os.js" ></script>
      <script type="text/javascript">

        function getImgByStat(status){
          switch(status.toLowerCase()){
            case 'recruiting':
            case 'enrolling by invitation':
              return '/images/check_icon.png';
            case 'active, not recruiting':
            case 'not yet recruiting':
              return '/images/yellow_triangle_icon.png';
            case 'completed':
            case 'suspended':
            case 'terminated':
            case 'withdrawn':
              return '/images/X_icon.png';
            default:
              return null;
          }          
        }

        function getOrderNumByStat(status){
          switch(status){
            case 'Recruiting':
              return 1;
            case 'Enrolling by invitation':
              return 2;
            case 'Active, not recruiting':
              return 3;
            case 'Not yet recruiting':
              return 4;
            case 'Completed':
              return 5;
            case 'Suspended':
              return 6;
            case 'Terminated':
              return 7;
            case 'Withdrawn':
              return 8;
            default:
              return 9;
          }        
        }

        function getAPIUrl(callback){
          var fullname, baseUrl = "http://scctsi-ctds-production.herokuapp.com/1/clinical_trials/";
          osapi.jsonld.getOwner().execute(function(result){
            fullname = result.jsonld['prns:fullName'];

            // for testing only
            if(fullname === "chikwan") fullname = "Agustin Garcia";
            if(callback)
              callback(fullname.length ? baseUrl + "?pi_full_name=" + fullname : null);
          }); 
        }

        function getAPIData(callback){
          getAPIUrl(function(url){
            if(url !== null){
              $.getJSON(url, function(result){
                if(callback)
                  callback(result);
              });  
            }
          });
        }

        function getAppData(callback){
          osapi.appdata.get({'userId':'@owner',
            'groupId':'@self',
            'appId':'@app',
            'fields':'ct_data'
          }).execute(function(result){
            if(callback)
              callback(result);
          });
        }

        function loadTrialOrder(callback){
          getAPIData(function(apiData){
            getAppData(function(appData){
              if(!apiData.error && !appData.error){
                var viewer = os.osapi.getViewerFromResult(appData);
                var trials = [];

                if(apiData.count){
                  sortTrials(apiData.response);
                  if($.isEmptyObject(viewer)){
                    for(var i=0; i<apiData.response.length; i++){
                      var trial = {};
                      trial.i = '' + apiData.response[i].id;
                      trials.push(trial);
                    }
                  } else {
                    var trialObj = JSON.parse(viewer.ct_data);
                    var newTrials = findNewTrial(trialObj, apiData.response)
                    if(newTrials.length){
                      trials = trialObj.slice();
                      for(var i=0; i<newTrials.length; i++){
                        var trial = {};
                        trial.i = '' + newTrials[i];
                        trials.push(trial);
                      }
                    }
                  }

                  if(trials.length){
                    osapi.appdata.update({'userId':'@owner',
                      'groupId':'@self',
                      'appId':'@app',
                      'data':{'ct_data':JSON.stringify(trials)}
                    }).execute(function(response){
                      if(callback){
                        callback(trials, apiData);
                      }
                    });  
                  } else {
                    if(callback){
                      callback(JSON.parse(viewer.ct_data), apiData);
                    }
                  }
                }
              }
            });
          });
        }

        function sortTrials(trials){
          if(trials.length > 1){
            trials.sort(function(a,b){
              if(a.recruitingStatus === b.recruitingStatus)
                return new Date(b.studyCompletionDate) - new Date(a.studyCompletionDate);
              return getOrderNumByStat(a.recruitingStatus) - getOrderNumByStat(b.recruitingStatus);
            });
          }
        }

        function findNewTrial(dbArr, apiArr){
          var hash = {}, diffArr = [];
          for(var i=0;i<dbArr.length;i++){
            hash[dbArr[i].i] = true;
          }

          for(var i=0;i<apiArr.length;i++){
            if(hash[apiArr[i].id] !== true){
              diffArr.push(apiArr[i].id);
            }
          }
          return diffArr;
        }

        function findTrial(id, jsonObj){
          for(var i in jsonObj.response){
            if(jsonObj.response[i].id == id){
              return jsonObj.response[i];
            }
          }
          return null;
        }

        function saveTrialOrder(arrayId){
          var trials = [];
          for(var i=0; i<arrayId.length; i++){
            var trial = {};
            trial.i = arrayId[i];
            trials.push(trial);
          }
          osapi.appdata.update({
            'userId':'@owner',
            'groupId':'@self',
            'appId':'@app',
            'data':{'ct_data':JSON.stringify(trials)}
          }).execute(function(response){
          });
        }


        function displayData(orderList, jsonObj){
          for(var i=0; i<orderList.length; i++){
            var t = findTrial(orderList[i].i,jsonObj);
            if(t !== null){
              renderTrial(t.id, t.officialTitle, t.recruitingStatus, t.diseaseArea, t.siteStatus, t.url);
            }
          }
          gadgets.window.adjustHeight(1500);
        }

        function renderTrial(pid, title, recStat, disease, siteStat, url){
          var li, div;

          li = $("<li/>",{id:pid, class:"trial"}).appendTo($("#trial-list"));
          $("<img/>",{class:"icon-eye", src:ENV_LOCAL_URL + "/images/eye_icon.png"}).appendTo(li);
          $("<img/>",{class:"icon-star", src:ENV_LOCAL_URL + "/images/star_filled.png"}).appendTo(li);
          $("<div/>",{class:"title"}).prepend($("<a/>",{id:pid, href:$(url).attr("href"), target:"_blank", text:title}))
          .appendTo(li);
          
          div = $("<div/>",{class:"detail"}).appendTo(li);
          if(recStat.length) $("<img/>",{class:"statImg", src:ENV_LOCAL_URL + getImgByStat(recStat)}).appendTo(div); 
          
          $("<span/>",{class:"status", text:recStat}).appendTo(div);
          $("<span/>",{class:"disease", text:" | DISEASE AREA: " + disease}).appendTo(div);
          $("<span/>",{class:"site", text:" | " + siteStat}).appendTo(div);
        }

      </script>
    ]]>
  </Content>

  <Content type="html" view="profile" preferred_width="700">
    <![CDATA[     
      <script type="text/javascript">

        var gadgetPath = ENV_LOCAL_URL;
        function init(){
          loadTrialOrder(function(result,result2){
            displayData(result,result2);
          });
        }

        function renderClinicalTrial(pid, title, recStat, disease, siteStat, url){
          var li, div;

          li = $("<li/>",{id:pid, class:"trial"}).appendTo($("#trial-list"));
          $("<a/>",{id:pid, class:"title", href:$(url).attr("href"), target:"_blank", text:title}).appendTo(li);
          
          div = $("<div/>",{class:"detail"}).appendTo(li);
          if(recStat.length) $("<img/>",{class:"statImg", src:ENV_LOCAL_URL + getImgByStat(recStat)}).appendTo(div); 
          
          $("<span/>",{class:"status", text:recStat}).appendTo(div);
          $("<span/>",{class:"disease", text:" | DISEASE AREA: " + disease}).appendTo(div);
          $("<span/>",{class:"site", text:" | " + siteStat}).appendTo(div);
        }

        gadgets.util.registerOnLoadHandler(init);
      
      </script>

      <div id='debug'></div>
      <div class="profile-view">
        <div class="gadgetContainer">
          <div>
            <span class="HeaderImage"><img src="images/microscope_iconR.png"></span>
            <span class="HeaderTitle">Active Clinical Trials</span>
          </div>
          <ul id="trial-list">
          </ul>
        </div> 
      </div>
    ]]>
  </Content>  

  <Content type="html" view="home" preferred_width="700">
    <![CDATA[
     
      <script type="text/javascript">

        var gadgetPath = "https://s3.amazonaws.com/clinical-trials-gadget/xml/";
        var elemList = "sortable",
          elemTrial = "li.trial",
          classTrial = "trial",
          classTrialHidden = "trial-hidden",
          classIconEye = "icon-eye",
          classIconStar = "icon-star",
          urlPath = "https://s3.amazonaws.com/clinical-trials-gadget/xml/";

        function init(){
          loadTrialOrder(function(result,result2){
            displayData(result,result2);
          });
        }

        gadgets.util.registerOnLoadHandler(function(){
          $(document).ready(function() {

            $("#trial-list").sortable({
              axis: "y",
              scroll: true,

              start: function( event, ui ) {
                ui.placeholder.height(ui.item.height());
              },

              update: function(event, ui) {
                saveTrialOrder($(this).sortable('toArray'));
              },

              items: "li:not(." + classTrialHidden + ")",
              cancel: "." + classTrialHidden
            });

            $( "."+classIconEye ).click( function( event ){
              event.preventDefault();
              var $trial = findTrialElement( $(this) );
              if ( $trial !== null ) {
                processTrialEyeClick( $trial );
              }
            });

            $( "."+classIconStar ).click( function( event ){
              event.preventDefault();
              var $trial = findTrialElement( $(this) );
              if ( $trial !== null ) {
               processTrialStarClick( $trial );
              }
            });

            init();
          });  
        });
        
      </script>
      
      <div id="debug"></div>
      <div class="home-view">
        <div class="clinicalTrialGadget">
          <div>
            <span class="HeaderImage"><img src="images/microscope_iconR.png"></span>
            <span class="HeaderTitle">Active Clinical Trials</span>
          </div>
          <ul id="trial-list">
          </ul> 
        </div>
      </div>
    ]]>
  </Content>
</Module>
